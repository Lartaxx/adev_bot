"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCliConfig = void 0;
const path_1 = require("path");
async function getCliConfig(options) {
    const pathFile = (0, path_1.join)(process.cwd(), "cli-config.json");
    const config = await Promise.resolve().then(() => require(pathFile));
    options = {
        ...options,
        config,
    };
    return options;
}
exports.getCliConfig = getCliConfig;
