"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveHandlersDir = void 0;
function resolveHandlersDir(options) {
    if (options.addType === "command" &&
        (options.commandType === "ApplicationCommand" ||
            options.commandType === "MessageCommand"))
        return (options.config.handlers?.commands ||
            options.config.handlers?.applicationCommands ||
            options.config.handlers?.messageCommands);
    if (options.addType === "event")
        return options.config.handlers.events;
    if (options.addType === "button")
        return options.config.handlers.buttons;
    if (options.addType === "selectmenu")
        return options.config.handlers.selectMenus;
    if (options.addType === "inhibitor")
        return options.config.handlers.inhibitors;
}
exports.resolveHandlersDir = resolveHandlersDir;
