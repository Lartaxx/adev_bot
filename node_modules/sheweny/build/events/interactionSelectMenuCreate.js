"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const errors_1 = require("../errors");
async function run(client, interaction) {
    try {
        if (!client.collections.selectMenus)
            return;
        const selectMenu = client.collections.selectMenus.find((value) => value.customId.includes(interaction.customId));
        if (!selectMenu)
            return;
        if (selectMenu.before)
            await selectMenu.before(interaction);
        const inhibitors = client.collections.inhibitors?.filter((i) => i.type.includes("SELECT_MENU") || i.type.includes("ALL"));
        if (inhibitors && inhibitors.size) {
            const sorted = [...inhibitors.values()].sort((a, b) => b.priority - a.priority);
            for (const i of sorted) {
                if (!i.execute(client, interaction))
                    return i.onFailure(client, interaction);
            }
        }
        await selectMenu.execute(interaction);
    }
    catch (e) {
        new errors_1.ShewenyError(client, e);
    }
}
exports.default = run;
